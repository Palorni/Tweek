# Arquivo .gitignore configurado para rastrear apenas o executável.

# 1. Ignora TUDO no diretório atual e subdiretórios
*

# 2. EXCEÇÃO: Permite que os diretórios sejam rastreados
# (O Git precisa rastrear diretórios para "entrar" neles)
!*/

# 3. EXCEÇÃO: Mantém o próprio arquivo .gitignore
!.gitignore

# 4. EXCEÇÃO CHAVE: Inclui o seu executável específico
# **Substitua 'Tweek.exe' pelo nome real do seu arquivo executável.**
!tweek.exe